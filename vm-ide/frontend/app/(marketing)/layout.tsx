import React from "react";

// Auth-only layout — marketing pages live at vinexus.space (marketing/)
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
